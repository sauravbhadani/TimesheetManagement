# .NET build output
bin/
obj/
*.user

# Visual Studio / Rider
.vs/
.idea/
*.suo

# EF Core migrations bundle output (if ever produced)
*.dacpac

# Local secrets never live in the repo — see README (dotnet user-secrets)
appsettings.*.local.json
